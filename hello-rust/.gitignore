# Generated by Cargo
# will have compiled files and executables
/target/

# Remove Cargo.lock from packages with path dependencies
# It's recommended to keep Cargo.lock for libraries to ensure reproducible builds, 
# but if your project is a binary, you can include Cargo.lock to ensure exact versions.
# For libraries, generally you can ignore it when you use path dependencies.
Cargo.lock

# Rust specific files
**/*.rs.bk
**/*.rs.swp

# Ignore the output of `cargo doc`, the documentation directory.
/doc/

# If you're using the `cargo-edit` extension, this file can be generated when adding dependencies.
.cargo_vcs_info.json

# Backup files created by Rust's `rls` (Rust Language Server)
.rls/

# Ignore build artifacts for WebAssembly
/wasm/target/
/wasm/pkg/

# IDE/Editor specific files

# Visual Studio Code
.vscode/
*.code-workspace

# JetBrains IDEs (IntelliJ, CLion, etc.)
.idea/
*.iml
*.sublime-project
*.sublime-workspace

# Other editors
*.swp
*.swo
*.sublime-project
*.sublime-workspace
*.vscode/
*.kate-swp
